id: ecommerce-data
version: "1.0.0"

profiles:
  guest_buyer:
    provider: static_json
    file: data/guest-buyer.json
  registered_buyer:
    provider: account_pool
    name: ecommerce-buyer-accounts
  random_shipping_address:
    provider: faker
    locale: zh_CN
    fields:
      receiver: person.name
      phone: phone_number
      address: address
