{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "skill-manifest.schema.json",
  "title": "SkillManifest",
  "description": "NEW (v3.1) — Skill Package 的 MANIFEST.yaml 格式规范",
  "type": "object",
  "required": ["name", "version", "description"],
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^mpt-[a-z]+-[a-z-]+$"
    },
    "version": { "type": "string" },
    "description": { "type": "string" },
    "entry_script": {
      "type": ["string", "null"],
      "description": "主入口脚本相对路径（null 表示仅知识型 Skill）"
    },
    "knowledge_files": {
      "type": "array",
      "items": { "type": "string" },
      "description": "知识库文件相对路径列表"
    },
    "input_schema": {
      "type": ["string", "null"],
      "description": "输入契约 schema 文件名（schemas/ 下）"
    },
    "output_schema": {
      "type": ["string", "null"],
      "description": "输出契约 schema 文件名（schemas/ 下）"
    },
    "requires_node": {
      "type": "boolean",
      "default": false,
      "description": "是否需要 Node.js 运行时（.cjs 脚本）"
    }
  }
}
