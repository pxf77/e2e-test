# Flow Tree Pruning

`mpt-reg-path-extract` accepts merged cases and converts them into:

- one normalized `regression_flow`
- one or more `regression_paths`
- governance warnings

The skill is contract-ready for future sample adapters. Real `.xmind` or richer business flow sources can replace the local heuristics as long as the output fields remain stable.
