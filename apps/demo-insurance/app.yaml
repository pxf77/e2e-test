id: demo-insurance
name: Demo Insurance
domain: insurance

requirements:
  prd: requirements.md
  manual_cases: manual-cases.json

entrypoints:
  web:
    base_url: https://example.com
    start_url: /product/demo-insurance

page_models:
  dir: page-models

data:
  pack: data/data-pack.yaml

execution:
  default_runner: playwright
  environments:
    local: envs/local.yaml
