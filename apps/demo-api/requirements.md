# Demo API Requirements

The API regression workflow accepts declarative HTTP scenarios through `inputs.api_scenarios`, verifies status and JSON/body expectations, and writes response evidence into the run Artifact Manifest.
