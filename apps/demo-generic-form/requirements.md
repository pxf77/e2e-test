# Demo Generic Form Requirements

This demo validates the generic-web Domain Pack and the v2 App Pack contract. It intentionally uses `https://example.com` as a stable placeholder entrypoint for static validation.
